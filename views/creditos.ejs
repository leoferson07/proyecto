<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Historial de Créditos</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        h1 {
            color: #333;
            font-weight: 700;
        }
        .card-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
            width: 100%;
            max-width: 1200px;
        }
        .card {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 300px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .card h2 {
            margin: 0;
            color: #007BFF;
            font-size: 24px;
        }
        .card p {
            margin: 10px 0;
            color: #333;
            font-size: 16px;
        }
        .card .monto {
            font-weight: 700;
            color: #4CAF50;
        }
    </style>
</head>
<body>
    <h1>Historial de Créditos Otorgados</h1>
    <div class="card-container">
        <% creditos.forEach(history => { %>
            <div class="card">
                <h2>ID: <%= history.id %></h2>
                <p class="monto">Monto de Crédito: $<%= history.montoDeCredito %></p>
                <p>Nombre del Usuario: <%= history.nombreDelUsuario %></p>
                <p>Fecha de Pago: <%= history.fechaDePago %></p>
            </div>
        <% }) %>
    </div>
</body>
</html>